{
  "name": "todoist-orbit",
  "description": "Operate Todoist through a deterministic stdlib-only Python CLI backed by the Todoist REST API",
  "version": "0.1.2",
  "credentials": [
    {
      "name": "TODOIST_API_KEY",
      "description": "Todoist API token from https://todoist.com/app/settings/integrations/developer",
      "required": true,
      "sensitive": true
    }
  ],
  "optional_env": ["TZ"],
  "install": {
    "type": "manual",
    "note": "Copy to ~/.openclaw/skills/todoist-orbit/ for a user-local install"
  }
}
